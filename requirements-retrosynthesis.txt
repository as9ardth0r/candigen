# Dépendance de scripts/run_retrosynthesis.py — séparée de requirements.txt
# car lourde (modèle + stock de précurseurs téléchargés à part via
# `download_public_data`, mis en cache entre les runs CI via actions/cache,
# jamais committés dans le dépôt).
aizynthfinder>=4.4
